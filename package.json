{
  "name": "n9-node-utils",
  "version": "1.0.0",
  "description": "Neo9 Node Utils Module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist/src"
  ],
  "scripts": {
    "build": "rimraf dist/ && tsc",
    "lint": "tslint src/*.ts",
    "test": "npm run lint && npm run build && nyc --reporter=lcov ava --verbose dist/test/",
    "preversion": "npm test",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "ssh://git@scm.bytefactory.fr:2222/n9node/n9-node-conf.git"
  },
  "author": "Sebastien Chopin <sebastien.chopin@neo9.fr>",
  "devDependencies": {
    "@types/node": "^7.0.18",
    "ava": "^0.19.1",
    "nyc": "^10.3.2",
    "rimraf": "^2.6.1",
    "tslint": "^5.2.0",
    "typescript": "^2.3.2"
  }
}
